Be careful to maintain consistency across these tree files: 
	DSLTrans.ecore -> The model itself.
	DSLTrans.emf -> Used to add notations to generate the graphical editor.
	DSLTrans.ecorediag -> Mostly used to change the model.
